import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
    initialMessages: [createChatBotMessage(`Bienvenido a Kapa 7 bot, Tienes preguntas acerca de ciberseguridad?`)],
    botName: "KAPA 7 Bot",
};

export default config;